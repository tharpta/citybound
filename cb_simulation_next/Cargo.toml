[package]
name = "cb_simulation_next"
version = "0.1.0"
edition = "2024"
publish = false

# Quarantined historical research. Keep independent from Citybound's production
# workspace and runtime unless a new accepted ADR authorizes reuse.
[workspace]

[dependencies]
bevy_ecs = "0.19.0"
