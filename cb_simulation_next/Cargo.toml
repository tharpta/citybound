[package]
name = "cb_simulation_next"
version = "0.1.0"
edition = "2024"
publish = false

# Keep the modern migration core independent from Citybound's historical
# nightly workspace until it can replace the legacy runtime.
[workspace]

[dependencies]
bevy_ecs = "0.19.0"
